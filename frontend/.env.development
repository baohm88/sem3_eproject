VITE_API_BASE_URL=http://localhost:5000/api

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL, // ✅ dùng đúng biến bạn đã set
  withCredentials: true, // nếu backend cần cookie
});

export default api;